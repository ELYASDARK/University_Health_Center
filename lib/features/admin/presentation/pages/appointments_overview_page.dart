import 'package:flutter/material.dart';

/// Page for viewing appointments overview
class AppointmentsOverviewPage extends StatelessWidget {
  const AppointmentsOverviewPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Appointments Overview')),
      body: const Center(child: Text('Appointments overview coming soon')),
    );
  }
}



